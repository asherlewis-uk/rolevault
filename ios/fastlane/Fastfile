default_platform(:ios)

platform :ios do
  desc "Build and upload to TestFlight"
  lane :beta do
    setup_ci(force: true)
    
    if is_ci
      increment_build_number(build_number: ENV["GITHUB_RUN_NUMBER"], xcodeproj: "RoleVault.xcodeproj")
    else
      increment_build_number(xcodeproj: "RoleVault.xcodeproj")
    end
    
    match(type: "appstore", readonly: true)
    
    build_app(
      scheme: "RoleVault",
      export_method: "app-store",
      xcargs: "CODE_SIGN_STYLE=Manual PROVISIONING_PROFILE_SPECIFIER=match%20AppStore%20com.rolevault.app"
    )
    
    upload_to_testflight(skip_waiting_for_build_processing: true)
  end
end
